export enum ItemType {
  FoodIngredient = 'FoodIngredient',
  Meal = 'Meal',
  Drink = 'Drink',
}

export enum ActivityType {
  GoTo = 'goTo',
  Eat = 'eat',
  Cook = 'cook',
  Sleep = 'sleep',
}

export enum ObjectiveType {
  HaveMeal = 'haveMeal',
  Rest = 'rest',
}
